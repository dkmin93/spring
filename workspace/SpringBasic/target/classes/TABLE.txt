--계정 생성
CREATE USER SPRING IDENTIFIED BY SPRING;

--권한
GRANT RESOURCE, CONNECT TO SPRING;

--테이블 스페이스
ALTER USER SPRING DEFAULT TABLESPACE USERS QUOTA UNLIMITED ON USERS;

CREATE TABLE SCORE (
    SNO NUMBER,
    NAME VARCHAR2(30),
    KOR VARCHAR2(30),
    ENG VARCHAR2(30),
    MATH VARCHAR2(30)
    );
    
ALTER TABLE SCORE ADD CONSTRAINT SCORE_PS PRIMARY KEY (SNO);
CREATE SEQUENCE SCORE_SEQ NOCACHE;

SELECT * FROM SCORE ORDER BY SNO DESC;